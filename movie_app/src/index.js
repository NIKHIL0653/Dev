import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// This line will create a root object and render the App component into the root object
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
